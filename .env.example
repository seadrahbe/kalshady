SP_ENV=dev
SP_DATABASE_URL=postgresql+asyncpg://shadypredict:shadypredict@localhost:5433/shadypredict
SP_BANK_API_URL=http://127.0.0.1:8021      # https://bucks.shady.tel for live
SP_HOUSE_PAN=8997000000000002              # the ShadyBucks account deposits land in
SP_HOUSE_PIN=4242                          # local dev only; live house uses a runtime OTP
SP_ADMIN_KEY=change-me                     # /admin password
SP_FERNET_KEY=                             # generate: python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())"
